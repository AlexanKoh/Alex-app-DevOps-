# My Flask App

REST API with Flask and MySQL

## Quick Start

```bash
docker-compose up -d
curl http://localhost:5000/health
```

## API Endpoints

- GET /health - Health check
- GET /users - List users  
- POST /users - Create user
- GET /db - Database info
